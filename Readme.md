# Start application
ssh -D localhost:6443 -N steppingstone

if hardcoded: rebuild website on submission..
Best Practice: Hybrid Approach (Server-side Rendering + Caching)
- Implement caching using tools like Flask-Cache or a CDN.

So: first do it dynamically and see the perfomance that that yields.