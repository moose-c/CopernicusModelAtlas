frontend:
- model pages.
- editing also removes the icon (so probably messes up all byteas)

difc files:
- database:
  - retrieval of lo's 
-api:
  - no server clog.
- fe:
  - perform check after submission


Log in:
- Make fe behind log wall
  - User pages behind log wall
  - allow to submit and preview
- Api behind log wall
  - also give the user to the api to allow users to view their own models.  
