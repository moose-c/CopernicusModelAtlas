difc files:

Log in:
- Make fe behind log wall
  - User pages behind log wall
  - allow to submit and preview
- Api behind log wall
  - also give the user to the api to allow users to view their own models.  


WH:
- allow modellers to input text including bold and \n etc
- side bar?
- long description = short description if left empty
- preview field for images: 
<img
              src={`data:image/png;base64,${formData.icon}`}
              alt="Preview"
              width="100" // Set the desired preview size
              height="100" // Set the desired preview size
            />

-	Ook scenarios 
-	Uncertainty bands
-	Xxx
- first value of every variable is the one that is shown


MH:
- improve connection for extended period to keep alive during file streaming