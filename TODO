frontend:
- Fine tune form:
  - browse should have some indication that something is entered.
- check form on:
  - required fields are filled out.

database:
- storing images, csv and tif is better as bytea, whilst geojson is better as jsonb
- alternative method: storing not files but file PATHS. 


Log in:
- Make fe behind log wall
  - User pages behind log wall
  - allow to submit and preview
- Api behind log wall
  - also give the user to the api to allow users to view their own models.  
